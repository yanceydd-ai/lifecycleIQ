# Database (used by both db and api containers)
POSTGRES_USER=lifecycleiq
POSTGRES_PASSWORD=changeme_use_strong_password

# API
JWT_SECRET=changeme_use_a_long_random_string
JWT_EXPIRES_IN=7d

# Web — set to the exact URL users type into their browser (IP or hostname, with port if not 80/443)
NEXTAUTH_URL=http://192.168.1.100:3000

# URL of the NestJS API (set to your Railway/Render/Fly.io deployment URL in production)
API_URL=http://localhost:3001
AUTH_SECRET=changeme_use_a_long_random_string_min_32_chars
AUTH_TRUST_HOST=true

# Microsoft Entra ID SSO (leave blank to disable SSO)
AUTH_ENTRA_CLIENT_ID=
AUTH_ENTRA_CLIENT_SECRET=
AUTH_ENTRA_TENANT_ID=

# Shared secret between the web app and API for SSO token exchange
SSO_INTERNAL_SECRET=changeme_use_a_long_random_string_min_32_chars

# Email alert notifications (leave SMTP_HOST blank to disable)
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_FROM=lifecycleiq@yourdomain.com
ALERT_TO_EMAIL=itadmin@yourdomain.com
ALERT_CRON_SCHEDULE=0 8 * * *
